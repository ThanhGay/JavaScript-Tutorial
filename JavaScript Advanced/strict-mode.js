"use strict";           // Sử dụng chế độ nghiêm ngặt cho toàn bộ file .js
                        // và cần đứng đầu file

fullname = 'Nguyen Van A';

function testFunc() {
    'use strict'            // hoặc cố thể dùng 'use strict' chỉ cho phạm vi hàm
    age = 18;
}

testFunc();

console.log(fullname);
console.log(age);